import { StyleSheet } from "react-native";

const globalStyles = StyleSheet.create({
    header: {
        fontSize: 30,
        marginBottom: 10,
    },
    subheader: {
        margin: 10,
        fontSize: 20,
    },
});

export default globalStyles;
